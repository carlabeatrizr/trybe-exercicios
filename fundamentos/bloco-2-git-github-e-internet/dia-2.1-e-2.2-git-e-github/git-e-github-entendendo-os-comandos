Git e Github - dia 2.1 e 2.2
Aprendendo e entendendo os principais comandos Git e como utiliza-los junto com o Github:

# Comandos:

Clonar um repositório remoto para o seu repositório local: git clone <url-do-repositorio>

Lista todas as branchs do seu repositório local:
git branch

Cria uma nova branch a partir da branch atual e já faz checkout para a mesma:
git checkout -b <nome-da-branch>

Muda da branch atual para outra branch com um nome específico:
git checkout <nome-da-branch>

Consegue visualizar informações e modificações sobre a branch e arquivos dentro dela:
git status

Cria uma mensagem especifica sobre as alterações que foram feitas na branch:
git commit -m "mensagem que gostaria"

Você pode adicionar todos os arquivos que você modificou usando: git add .

Você pode adicionar um arquivo específico que modificou usando:
git add <nome-do-arquivo>

Você pode enviar e salvar sua branch do repositório local para o remoto usando:
git push -u origin <nome-da-branch>
Após o primeiro push da sua branch, você poderá usar apenas o comando: git push.

Você pode atualizar e pegar a versão de uma branch do seu repositório remoto e trazer para o seu repositório local:
git pull 